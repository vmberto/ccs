int main() {

    int a = 1, b = 1, c = 1;

    int d = 1, e = 1, f;
    
    int g = 1, h, i;

    int j, k, l;

    float m = 1.5, n = 1;

    char o = 'a';

    a = 1, b = 1, c = 1;

    d = 1, e = 1, f;
    
    g = 1, h, i;

    o = 'a';

    a = ((1) + (1) - ((1 - 1) * 1));

    b = ((1 + a) + (1 + a) - ((1 - (a - 1)) * 1));

    c = (1 + 1 + a + b) / (a + b);

    d = (5 + 5 + (a * b)) / 2;

}